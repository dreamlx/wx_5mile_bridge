<div class="col-md-10">
  <h3><%= t(:appointment_list) %></h3>
</div>
<div class="col-md-2">
  <%= link_to t(:new_appointment), new_appointment_path, class: "btn btn-danger" %>
</div>
<div class="col-md-12">
  <%= render 'search' %>
</div>
<div class="col-md-12">
  <hr />
  <table class="table">
    <thead>
      <tr>
        <% Appointment::HEADERS.each do |column| %>
          <th><%= Appointment.human_attribute_name(column) %></th>
        <% end %>
        <th colspan="4"><%= t(:operations) %></th>
      </tr>
    </thead>
    <tbody>
      <%= render @appointments %>
    </tbody>
  </table>
</div>